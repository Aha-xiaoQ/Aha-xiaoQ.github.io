# M01 改动记录

版本：terra-m01；日期：2026-09-16；本地工程交付，未推送。

提取八个实际接入的 ES 模块、33 个有明确顺序的兼容片段、原样式与页面模板、195 条内嵌媒体和46条音频记录。增加可重复构建、原始参考重建、测试、只读本地服务、源码打包和页面资料接入。

候选的 UI 范围：画面辅助说明从 1-1 修正为 1-3、允许页面缩放；原 HUD 和控件保留。

没有换引擎，没有引入城堡关，没有修改第一/二期独立发布文件，没有覆盖线上第三期，也没有更改原素材许可。

旧“开始隐藏场”测试钩子在当前底稿已不能直接进入隐藏房间。此次浏览器测试改为先放置于原管道口，调用真实进入流程，并断言确实到达隐藏场、召唤 Boss、取得奖励和返回；不以空操作相等冒充覆盖。
