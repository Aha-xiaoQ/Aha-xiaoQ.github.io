# M02 修改记录 · 2026-09-16

- 新增7个实际接入模块：bindings-model、mount-binding、keyboard-router、binding-capture、lifetime、settings-session、bindings-view。
- compat46 转为明确 bridge；mount 事务有原片段对照。主世界与战斗片段不重构。
- 默认控制配置回导修复；设置会话资源清理；过期导入隔离；绑定按钮名称。
- 模块契约、依赖/测试门禁、兼容包装预算、golden 保护、9项真实状态任务。
- 源码文档生成、任务计划输出、续接摘要、PR与Issue规范、Windows/Linux CI配置。
- 源码下载改为 M02，新页面链接与元数据同包；M01历史下载保留。

没有换引擎、没有加城堡、没有更改线上稳定版或扩大素材许可。
