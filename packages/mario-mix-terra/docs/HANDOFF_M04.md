# M04 交接与发布状态

唯一底稿见 baseline.json；本次从M03升级。当前版本元数据见 release.json，模块见 architecture/modules.json；不要把网站R编号当关卡编号。

本轮收尾不是无限扩张功能。优先完成PUBLISH.md真实设备与发布验收，再接受小PR。原第三期与前两期线上版本、城堡分支保持不动；下一位维护者不得重新引入已退回的大图标或独立网页外壳。

已实现：24个独立模块、4个宿主桥接、33个compat片段。物品规则实际接入候选；场景会话防护与数据契约已加固。真正通用的泰拉actor尚未迁出；公开资料不得称任意角色任意关卡可用。

先读 START_HERE → CONTRIBUTING → ARCHITECTURE → KNOWN_LIMITS → TEST_REPORT_M04 → PUBLISH。记录修改范围和行为差异，不通过关闭测试或改原校验值掩盖回归。

历史M01—M03资料原样保留，最新入口是本文件。完整环境验收、远端CI、首次外部PR和实际部署结果不得从已有文档推断为通过。本次未作GitHub写操作。
