# M03 / R11 交接

## 唯一底稿

继续使用用户上传第三期。baseline.json记录原HTML SHA，必须重建一致；不要替换为R29城堡或第一期。M02源码ZIP为上轮安装基线。本轮是关卡接入协议v0.1和原型，不是完成版1-4。

## 工作位置

新增内容：content/extensions/。协议与能力：docs/stages/CONTRACT.md、READINESS.md。核心会话：stage-session；实例逻辑：platform-stage；原型移动：platform-motor；统一入口：src/bridges/stages.bridge.js；原始第三期仍封闭。

## 已做与未做

22模块已打包接入，33兼容片段仍未完全解耦。双房间样例可选择两个几何角色，共享设置、固定时钟、音频和Canvas。新增清单自动发现并可从UI进入。1-3参考输出锁定但不是任天堂原版逐格证明。未做Tiled导入、所有地图、旧actor通用化、跨关存档/物品事务、原作1-4必要机关或外部PR。

## 下一步优先级

先在真实Windows/浏览器/hardware确认新选择器与设置不串输入；然后让非作者按文档新增一个小内容包并提交真实PR；接着迁出一个完整真实角色到可注入房间对象接口；有此能力后再处理原版1-4底图、火焰棒/Boss/桥斧终点与正式素材。不要只把更多代码切文件就声称障碍消失。

## 交付过程

verify、stage:check、完整脚本回放、新选择器浏览器测试；pack后从源码ZIP解压verify并重打包一致。网站入口按R10后续R11增量追加，旧M01/M02源包保留，不替换主站样式。远端提交/部署由用户验收后操作。准确结果见TEST_REPORT_M03，未运行的平台不能标为通过。
