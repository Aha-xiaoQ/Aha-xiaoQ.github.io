# M05 发布前界面验收

先`npm run verify`与`npm run prepublish:check`。候选SHA写入验收记录；没有完成的项目保留pending，不能复制自动测试数作为人工批准。

- 菜单：普通入口不出现几何实验按钮，6角色可选，开始/暂停/继续/重开可达，原权利声明可打开。
- HUD：6工具点击命中与图标一致，数量/生命/魔力/装备/防御/计时可读；Boss与拾取内容正确。世界尺寸、跳跃判定、掉落行为和原版对照。
- 设置：实际键鼠、手柄打开和关闭；滚动到最后动作；改键/导出/导入/复位；关闭后只恢复原有暂停。
- 显示：专注切换不重开/暂停；菜单Tab、Space、Enter、Escape不让角色误动作；大号字体不截断按钮；高对比状态明确；系统及手动减少界面动效生效。
- 存储：刷新后文字/对比/动效选择恢复；隐私模式禁止存储时仍可用且不假报保存；旧存档/绑定不被重置；专注模式刷新后恢复普通布局。
- 设备：320/390/768/1440 CSS px与低高度横屏，200%浏览器缩放、移动横屏；HUD小字号在真机检查，不用无溢出替代可读性。
- 集成：真实4193/4194 HTTP、静态网页与模块网页资源、实际全屏、音频、自然地表/隐藏场景通关；Windows与Pages加载。

自动用例覆盖输入样本与离线DOM，不能替代以上设备和内容验收。网站当前源码链接需指向M05压缩包；M04历史仍可下载。
