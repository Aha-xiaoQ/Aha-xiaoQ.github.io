# M03 验证记录

日期2026-09-17。状态：本地候选，未部署、未推送；新关卡API为原型。原始游戏为完整上传的第三期HTML与内嵌媒体；网站测试单独使用历史更新包提供的文本夹具，不能混称全站运行。

## 实际执行（最终包复核另附结果）

- 源码 Node：118项通过、0失败、0跳过（旧85＋新增33）。
- 新增场景用例覆盖：严格schema、ID/能力/驱动/音频key语法、房间/检查点/入口引用、拓扑不可达、只增overlay、不改底图、任意新ID脚手架、悬空目录链接、一次完成与退出清理、确定性原型移动、单向平台/二段跳、房间拾取保留、共享按键及逻辑音频。
- 原第三期完整脚本离线回放13阶段；13阶段无状态差异、无未捕获脚本错误。控制部分进入与伤害条件，不代表自然全通关。
- 新宿主浏览器：真实DOM选择器点击、键盘移动/暂停、共享M02设置、角色切换、场景退出和返回真正第三期。新增原生复选框空格测试，防止窗口按键泄漏到旧菜单。窗口320/390/768/1440宽度。
- 原设置窗口25项离线DOM回归通过；新选择器21项DOM检查通过。新增测试不是删除失败测试；一次初测发现后注册监听器无法拦住更早的菜单捕获，已改为复用原最早输入分流点。

## 限制

浏览器检查用set_content运行完整HTML，外部网络禁止，测试随机/步进受控；不是HTTP/ESM/真实输入设备或Windows实机验收。`platform-v1`测试的是新原型行为，不能用其通过证明原角色/原1-4通关。新地图schema验证不验证跳跃可达性、精确原版机制或素材许可。

没有真实外部开发者PR、全部原版地图、正式1-4、旧actor通用化、Tiled导入、全存档迁移；公开配方仍待实机验收。原第三期素材来源和可选远程依赖不变；不含字体文件。

新增匿名内容实际演练：独立副本使用CLI创建review-level和review-hero，仅改内容JSON，全src逐字节不变，再通过真实DOM选择并运行；7项演练检查通过。它由作者自动化模拟，不是实际外部开发者PR。
