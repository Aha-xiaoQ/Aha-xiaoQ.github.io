function t24AssignMount(binds,button){const next=structuredClone(binds),freed=next.mount.pad.filter(i=>i!==button);for(const [key,b]of Object.entries(next)){if(key==='mount'||!b.pad.includes(button))continue;b.pad=b.pad.filter(i=>i!==button);if(!b.pad.length){const available=[...freed,11,3,10,4,6,...Array.from({length:32},(_,i)=>i)].find(i=>i!==button&&!Object.entries(next).some(([k,v])=>k!=='mount'&&v.pad.includes(i)));if(available===undefined)return false;b.pad=[available];}}next.mount.pad=[button];Object.assign(binds,next);return true;}