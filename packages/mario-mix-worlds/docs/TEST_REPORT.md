# R21 / WorldKit W02 · 验证记录

日期：2026-09-19。地图工具 0.2.0 / W02；配套 M06 继续为 0.4.2。没有推送、部署或人工发布批准。

## 本次交付

补齐 2-1 至 8-4 的 28 关参考模板。合计 32 关、63 个参考区域、33 个条件路线分段，共 96 个 SVG / Tiled 对象层视图；32 个逐关包及 8 个世界包。底图按固定提交的社区实现转录，不是任天堂原作数据导出，也没有完成原版逐格或逐时序独立验证。

4-4、7-4、8-4 保留区域、before/stretch/after、入口、管道与条件分支，不压成已完成的固定通关路线。5 个水下区域保留模板，但不冒用 platform-v1 的重力作为游泳。世界 2–8 的运行入口是分区地形检查，不是 28 个完整游戏场景。

## 实际执行结果

|检查|结果|范围|
|---|---|---|
|WorldKit Node|73 项通过|保留 30 项基线并新增 43 项；28 关各自数据、坐标/宏展开、63 区域、33 分段、负间距、无限管道、条件目标、Tiled 唯一 ID、32+8 ZIP、创建草稿、路径防护，以及实际 Node HTTP 请求|
|网站 Node|312 项通过|既有网站、版本、内容、关卡投影与生成检查；更新明确的数量与缓存版本断言，未删除测试或关闭一致性检查|
|R21 安装器|24 项通过|真实 R20/W01 转换后的四类文本夹具；预检零写入、应用、生成、重复零写入、字节回退、迟到编辑、路径链接、同名文件、手改源码/目录/预览、下载与版本隔离|
|离线 Chromium|62 项断言通过|32 张实际地图选择与 SVG 解码、8 世界、条件分段、水下警告、缩放、重置、四档宽度、32 网站条目、40 分世界下载链接、96 个可聚焦预览区|
|M06 契约与构建|通过|WorldKit 测试实际调用原 M06 验证器，并在独立副本构建完整候选；原 M06 源码不改写。不是在本轮重跑原游戏 171 项完整测试，也不是自然通关|

重复宽度/布局不是独立游戏特性；不将不同组的断言简单相加充当游戏测试数量。最终归档的 CRC、文件哈希、解压重建、源码重打包、安装/重复/回退另见包旁 FinalVerification.json。

## 检查中修正

转录工具曾将 Infinity 读为非标准 JSON 数值，已改为保留原字符串。7-2 转录复用中的 thing 选择曾遮蔽 Fill 宏，已修正负向金币间距和原拼写字段的保留，回归覆盖。水下地图册不再给出会被平台工具拒绝的创建命令，而是说明环境驱动前提。

网站测试初次缺少真实 M06 源码下载元数据，补入原 R16 包的真实文件后复跑，不制造空文件。旧断言的第一世界数量按 32 关/96 预览更新；当前游戏版本与历史下载保护仍保留。

离线页面截图第一次未解析 site-shell.css 的 @import，因此呈现白底；仅修复测试加载器，展开实际主题 CSS 后重新检查和截图，不更改主站主题。隐藏 details 内的 command 使用 textContent 检查，避免把不可见的 innerText 当成空数据。

## 环境和边界

Linux、Node.js 22、系统 Chromium、Python Playwright。Node 本地 HTTP 服务真实请求通过。Chromium 直接访问本地 HTTP 返回 ERR_BLOCKED_BY_ADMINISTRATOR，未修改或绕过策略；浏览器组使用真实 HTML/JS/JSON/SVG 与显式内存 fetch、URL/history 替身。它不能证明真实 HTTP/ESM/CSP、权限、下载或站内转场通过。

网站外围为旧包提供并标明的文本测试夹具，不含完整主站品牌图片/字体；配套 M06 为真实源码。预览没有使用概念图或空白媒体使测试通过。没有新增官方人物、BGM、音效、ROM 或字体文件。已有 M06 内资源沿原包保留。

仍需人工验收：完整网站网络/下载/缓存、Windows 与 junction、真实手柄/触屏/读屏、每条原版路线保真、条件城堡与水下驱动、敌人/奖励/机关行为、自然通关、外部开发者 PR 和资源权利。缺失机制在每关 coverage 中保留，不标为已完成。
