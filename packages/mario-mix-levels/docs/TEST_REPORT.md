# K01 本地验证记录

2026-09-18。当前为地图开发模板，不是原版游戏发行版。

已执行：39 项 Node 检查，涉及四关八房间、坐标锚点、未实现对象、M06契约、场景运行、打包、数据导入/撤回、冲突与悬空链接防护；47 项离线浏览器检查；完整 M06 候选导入后16项真实选择器/键盘/暂停/退出检查。完整 M06 的 stage:check 与构建已执行，原始底稿重建 SHA 不变。

坐标参考按 FullScreenMario 固定提交转录，部分锚点与宏转换用自动断言复核。未逐格对照任天堂原版，更没有完成全部敌人/机关和自然通关验收。测试中的单房间入口是临时会话，只保留该房间可达的连接子图，不改变底图；原入场房间不会为了通过验证硬接回路。

浏览器HTTP被环境策略拒绝，未绕过。工作台在离线DOM中执行真实模块和数据；完整M06使用完整生成HTML、原测试标记和手动步进。网络、Windows、实体触屏/手柄、原版时序、屏幕阅读器与外部开发者PR仍待验收。

原始日志与最后ZIP解压检查见外层R20 validation和FinalVerification。没有将本地测试等同上线、游戏完整复刻或原作素材授权。
