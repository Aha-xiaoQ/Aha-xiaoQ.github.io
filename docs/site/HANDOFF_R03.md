# R03 · 开发记录分页面与阅读层级交接

记录日期：2026-09-15。交付类型：本地增量更新，未推送，未部署；不是游戏玩法版本。

## 本轮来自用户的要求

R02 统一性已改善；继续美化，分块和介绍更清楚。不要把大量详细内容挤在一页，让用户找不到重点。详细内容可以进入对应分页面。

## 完成的代码范围

- 概览页：当前重点、任务摘要、三个入口与试玩引导，原有长篇记录移到二级页面。
- 独立路径：任务、文档、模块、参考、更新、参与、交接。沿用主站路由，不增加另一套主题或一级导航。
- 任务分页：每页最多 8 项，保留搜索/筛选、完整任务信息、编辑与验收规则。
- 两种既有状态格式 A/B：不改共享 JSON、不改数据脚本正文、不改旧 HANDOFF；保留草稿键与导入导出。
- B 版状态构建器跟随新的运行清单位置；A 版打包白名单补齐新增页面。状态重新生成之后仍可以续接。
- 外层主站页面只调整修改脚本/样式的缓存版本，不替换正文；游戏运行文件、素材及下载包不在修改清单内。

## 基线说明

远端 main 经连接器查询仍为 `ed76f0b668a9e7c1a8d8f71bed2489cb2b063c9e`，不能把它当作已经应用 R02 的版本。本轮基于当前对话交付的 R02 更新包及用户确认的 R02 效果，应用器从用户本地已安装的 R02 中读取模板与状态。

两套状态同时存在或界面脚本已被独立编辑时，应用器停止，不猜测覆盖。新的安装记录位于 `docs/site/R03_INSTALLATION.json`。下一轮必须重新查询 main，与实际本地提交对齐。

## 下一步

1. 在完整仓库里检查八类视图、直接刷新、前进/后退与主站转场。
2. 任务编辑后切到文档再返回，核对本地草稿、导出内容及刷新恢复。
3. 分别运行原工作区的生成命令、检查和测试；确认生成后的新记录可以显示。
4. 记录真实提交/部署结果；然后继续源码基线对齐及游戏回归，不能把本轮页面更新算作游戏修复。

## 验证边界

具体执行和限制以 `TEST_REPORT_R03.md` 为准。制作环境受网络与浏览器导航策略限制；组件离线检查不替代真实来源的端到端验收。完整游戏、实体手柄、音频和 Windows 桌面仍需实机验证。

没有新增人物素材、字体文件或上游游戏代码。本轮不扩大任何既有授权。
