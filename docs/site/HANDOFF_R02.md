# R02 交接：让开发成为主站栏目

本轮需求：2026-09-15，用户再次指出独立协作界面不符合主站页面规范。修复范围是页面框架、共享样式与原站路由，不是游戏更新。

已实现：复用原生 shell 的 header/page-head/footer；开发加入原 content router；保留单一开发导航；旧入口兼容；移除独立配色、侧栏和自制 Q；新 CSS 只定义内容布局；开发组件离开时保留、返回时重挂，不重复初始化。兼容两个历史 R01 的状态格式，但不在两种格式间自动迁移。

不可遗漏：`docs/site/DEVELOPMENT_PAGE_SPEC.md`。这条规范已明确成为之后所有开发页面改动的验收条件。发布者应按 `TEST_REPORT_R02.md` 复验原站真实资源、字体、转场、移动布局和草稿持久化。

任务记录不重置：A 使用 `collab/project.json`；B 使用 `docs/mario-mix/project-state.json`。本轮不编辑任务 JSON、生成的数据脚本、原交接历史、游戏运行文件或素材。R01 是历史任务快照版本，R02 是界面整合版本。

安装记录：应用器会写入 `docs/site/R02_INSTALLATION.json`，记录检测到的 R01 类型、改动文件、应用前后哈希和保留的状态哈希。备份在仓库旁边，不放进公开站点。

发布状态：本包仅本地交付，没有远端推送、GitHub 设置或部署。下一轮先读取最新 main，确认维护者真正推送的版本，再读取实际状态源；不要重新交付一套同名 R01。

下一步：维护者应用、运行检查并验收主站集成；把实际 commit 与部署结果记进原有状态源。通过后再继续基线对齐和游戏模块化。
