# 玩家界面审阅与整改

审阅对象为真实M06源码与本地候选，不是线上完整体验评分。

|问题|代码位置与处理|
|---|---|
|第三期HTML还带第一、二期旧介绍，被多层脚本改写|增加候选专用player.template.html；player-copy集中当前标题、角色关卡与模式提示，保留原始模板供底稿重建。|
|固定像素HUD放大后文字粗糙，小屏一起缩到很小|hud-surface按显示像素绘制桌面文字，窄屏转为HTML重排。桌面点击坐标保留，窄屏调用同一工具槽。|
|帮助一律按泰拉角色写，按键重复且改键后过期|quick-guide根据实际角色和绑定返回提示；完整设置表恢复缺失的交互行。|
|拾取说明挤在游戏里；缺图时把中文名画成极小字符|拾取名称/效果转为HTML并保留最近8条；缺纹理图标仅使用中性图形，不伪装完整美术。|
|操作、音量、设备和开发说明混在侧栏|顶部统一入口，侧栏仅目标、当前动作与最近提示，资源状态进入设置详情，历史版权说明折叠在同一区域。|
|重开、切换与恢复默认容易误操作|增加确认、取消初始焦点、键盘及合成手柄边沿隔离；公开记录为行为修正，不伪装等价重构。|
|移走音频面板后销毁UI触发空节点报错|退出前恢复原兼容节点，完整原版/候选回放与终止事件测试不再报错。|

没有重做角色美术、地图、攻击判定、物品规则、碰撞或音序。历史世界内标签、真实全屏手柄和手机触控仍列入人工检查，不承诺全代码库不存在其他UI问题。
