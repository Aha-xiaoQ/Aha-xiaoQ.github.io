# M07 测试记录

2026-09-19；本地候选，未部署。真实底稿为M06/0.4.2；原始第三期SHA可重建一致。

源码 verify：196项通过（原171项与25项新增玩家界面检查）。完整脚本离线对照13阶段，无状态差异及未捕获错误。UI浏览器63项断言通过，覆盖6角色、确认、改键、提示、窄屏与退出清理。普通无测试开关模式另验证六角色真实启动与切换、默认配置菜单/设置/帮助与窄屏，没有未捕获错误。原始底稿SHA重建一致，prepublish自动检查通过，人工批准为false。网页更新器测试独立计数，不将文件数量或四档视口等当成独立功能数。

普通Chromium访问本地HTTP得到ERR_BLOCKED_BY_ADMINISTRATOR，未绕过。离线set_content运行完整候选与内嵌资源，远程请求拒绝。按键输入真实派发，部分游戏状态由原测试接口和手动步进设置；手柄是合成输入，不是实体控制器。截图另使用默认配置的干净候选，不展示注入测试说明。

修正过的失败：模块计数/测试夹具缺release.json、旧HUD固定显示假设与新窄屏策略不符、界面退出删除音频宿主导致迟到回调报错。普通无测试模式进一步发现动画回调引用内层UI变量，改为闭包安全的afterDraw桥接，并加入正常时钟回归。保留原行为对照、安全和损坏校验；没有删掉失败测试冒充通过。

未验证：Windows、真实HTTP/ESM/CSP、物理手柄/触屏、屏幕阅读器、全屏、刷新持久性、真实BFCache、音频听感和自然通关。没有FPS提升比例、可达性认证或发布批准。W02及已发布试玩保持字节保护。
