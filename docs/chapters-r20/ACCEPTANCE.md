# 验收清单

自动检查：`npm run check`、`npm test`；完整包 `npm run preview:build`。原 M06 源码不变，参考底图不被适配改写，所有生成文件可重建。网站 `worlds:sync` 后显示的状态与唯一目录源一致。

人工待验证：Windows Node/目录链接、真实浏览器 HTTP/CSP/资源加载、键盘/手柄/触屏、地图端点和房间自然往返、原作地形逐格与机关时序、文件下载、中文缩放/读屏、站点前进后退、外部首次PR。

1-2 循环平台尚未执行，1-4 火焰棒/Boss/桥斧尚未执行，因此不能因可打开预览就标记原版关卡可自然通关。标记依据与执行实现分开。

目录中的“可试玩”仅指原有三期发布入口，不指新模板已达到发行质量。0.1.0/W01为开发工具版本；0.4.2/M06是配套游戏版本。

网站重点：前三期试玩分别可达；1-4无虚假开始按钮；当前M06源包不变；三主要资料入口、小图标、主导航保留；8张SVG实际载入；四份小地图ZIP与完整Starter都有实际目标；历史任务/下载不丢失。
