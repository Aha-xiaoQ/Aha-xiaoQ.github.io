# R07 验收清单

## 本地自动
执行 `npm run site:check`、`npm run test:r07`、`npm run journal:check`、`npm run check`、`npm test`。`site:audit` 必须在完整仓库执行，缺失素材非零退出；外部图片标为未抓取，不冒充可用。

## 真实网站
游戏桌面统一双列；筛选只剩一项、奇数项仍半宽；680px 及以下单列。含字封面四角完整，卡片按钮始终可见，长中文英文不溢出。320/390/768/1440px 与 200% 文本缩放分别检查。

Q咪整块暖纸背景，无斜纸片、黄框、强纹理穿过主体；原始表只有第一行前六帧参与，播放两次归首帧。实际贴图的白底/透明度未核验时，不通过 CSS 色键假装消除。减少动态效果时精灵不动，键盘焦点清晰。

从首页→项目→游戏→工具→开发→返回前進，主站框架/转场不得变成第二套。禁用脚本后直接打开三类列表仍可读可进入；筛选不要求无脚本可操作。

验证旧任务、未导出草稿和 R05 工程，确认没有被覆盖。资源、语言、下载、真实动画必须在完整网站核验，不能由组件截图代替。
