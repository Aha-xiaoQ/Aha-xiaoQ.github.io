# R07 变更记录

## 内容组件
移除 R06 首张游戏跨列特例。统一两列/一列断点，取消像素角色以外的强制 pixelated。项目和游戏正文完整、动作贴底。图片使用 img 与 contain，不用背景 cover 裁切含字海报。

## 素材结构
新增独立验证器、六种 CSS 场景、稳定 ID 表现登记、首行优先/后续懒加载、失败占位。Q咪展示按原精灵表参数配置，组件不再包含项目名称分支；实际素材视觉仍需完整仓库验收。

## 源码组织
静态/动态列表共用函数，新增 site:build/check/audit。原 journal:build 增加公共依赖接入。根 check/test 追加新检查，不删除旧检查。增加保护式安装、回退、文件清单与本轮文档。

## 不在本轮
没有重新生成游戏背景，没有优化/重新分发大图，没有新的后台或路由，没有修改游戏行为，没有执行 Git 推送或远端配置。
