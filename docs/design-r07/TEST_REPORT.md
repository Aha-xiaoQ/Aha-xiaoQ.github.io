# R07 实际测试与交付记录

日期：2026-09-15。版本：workshop-r07。状态：本地增量交付，未推送、未部署。适用：已安装 R06 的完整网站；不把线上旧 main 或原游戏版本作为本包的可覆盖对象。

## 实际结果

| 检查 | 结果 | 边界 |
|---|---|---|
| 新增素材/卡片/构建测试 | 42 项通过，0 失败 | 契约、危险路径、转义、任意 ID、六种场景、静态生成、幂等、缺失文件、人工改动保护 |
| 原 R06 与 journal 回归 | 61 项通过，0 失败 | R06 13 项 + journal 48 项，不是 61 个新增功能 |
| 原 R05 工程回归 | 34 项通过，0 失败 | 原片段对照和合成宿主，不是完整游戏通关 |
| 安装器测试 | 21 项通过，0 失败 | 四种 R06 历史来源的检查/安装/重复安装/按字节回退，冲突、并发编辑、状态与游戏保护 |
| Chromium 离线组件检查 | 46 项断言通过，0 未捕获页面错误 | 列数、统一尺寸、筛选、失败占位、精灵窗口、减少动态效果、文字放大、原框架唯一 |
| 集合构建一致性 | changed: 0，pages: 3 | 二次生成字节稳定 |
| 开发栏目构建一致性 | changed: 0，removed: 0，pages: 22 | 既有项目状态，未改变其业务信息 |

原始日志在包内 validation/。四种输入兼容重复不累加为新的独立功能数量。根 npm test 的全部历史脚本并未都包含在夹具中，因此没有声称在用户完整仓库通过根命令。

## 测试输入说明

从当前会话真实的 R06 更新包恢复四份文本夹具，再应用 R06，生成已记录的 R06 输入。新增首页、项目、游戏、工具模板为明确的测试 HTML，代表性内容与游戏哨兵为夹具，不是完整下载的原站或游戏。R05 工程源码与旧任务来自已交付包；运行游戏文件与完整资源未下载。

当前环境只保留上一轮 R07 的两份规范稿，没有保留未交付源文件。本轮据 R06 和这两份规范重新完成并重新执行测试，以上数量不沿用上一轮 38 项/22 项的口头统计。

## 真实浏览器限制与截图

重新尝试 Chromium 打开 http://127.0.0.1:4197/projects/，得到 ERR_BLOCKED_BY_ADMINISTRATOR；未修改或绕过浏览器策略。随后使用 set_content 离线组件环境，加载原站 CSS 节选与本轮真实脚本，通过直接调用已有 SITE_SHELL_RENDER 验证组件重绘，而不是模拟通过了原生 router。

截图使用用户提供的海报截图裁片和私有字体，仅检验几何布局；样例文字有明确“布局测试”提示，图片可重复，不是实际网站完整目录。原始 Q咪精灵表无法下载，使用标记 TEST 的合成精灵表检查帧窗口，不用它宣称已验证真实小猫图像或动画。私有字体、截图裁片、合成精灵表均不进入 payload；截图是像素结果，不含字体文件。

## 不能据此声称完成

完整网站 HTTP/ESM/CSP 加载、原生转场实际呈现、前进后退、真实字体/背景、原始 Q咪贴图与时间序列、浏览器持久草稿/下载、Windows 原生 CMD 与权限、GitHub CI/Pages 都仍须维护者确认。本包没有重新授权素材，没有完成大图压缩，没有改变游戏逻辑，也没有证明整个网站达到某项无障碍认证。

## 本轮修正过的问题

URL 校验最初拒绝合法目录链接，已支持末尾单个斜杠但仍拦截内部穿越。构建首轮生成标签的顺序与 metadata 空白造成非幂等，已统一。静态模板外部的手改内容可能被误收录为生成结果，已改为先对比旧哈希再判断输出一致。CSS 修改最初覆盖了减少动态效果声明，已恢复并用真实 computedStyle 检查。安装测试最初把允许修改的 games/index.html 当作游戏运行文件，已限定保护对象为实际游戏子目录和 play 文件，保留写入清单断言。

## 最终包

最终 ZIP 在制作后另行重新解压，核对 CRC 与 SHA256SUMS，执行解压目录内安装器测试和一次完整应用/检查/重复应用/回退。结果另存为 PixelWorkshop_R07_FinalVerification.json，不能用仅源目录检查代替最终解压复验。

## 维护者复跑

`npm run site:build` → `npm run site:check` → `npm run site:audit` → 原 `npm run check`、`npm test`。真实素材缺失会令 audit 非零退出，应解决路径，不把旧文件倒退覆盖。查看 ACCEPTANCE.md 后验收并记录真实提交与部署结果。
