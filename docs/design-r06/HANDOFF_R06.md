# R06 交接

交付类型：本地增量更新，**不是已推送/已部署**。源码基线为已交付的 R04 多项目框架；R05 没有改动这一轮替换的框架源码，因此两者均可安装。连接器重新读取到的公开 main 仍是 `ed76f0b668a9e7c1a8d8f71bed2489cb2b063c9e`；不假定用户本地未推送代码已在远端。

## 已实施
- 作品卡片采用黑/米白节奏、红色错位层、独立图形区、单一主要动作；正文保持水平。
- 项目为双列作品卡；游戏为首项宽卡与后续双列；工具为正文/预览双区。
- 开发目录保留多项目框架，用项目选择、两条近期记录与参与入口组织首页。
- 任务、资料、更新、参与、管理依然在各自页面；保留筛选、分页和旧编辑器入口。
- 顶栏、首页快捷入口、面包屑等当前导航统一“开发 / Dev”；历史日志正文不全局替换。
- 沿用原路由动画，修正模块与页面的版本化引用，防止新旧渲染器混载。

## 不得误报
不是完整网站下载副本。测试与预览如使用夹具、主题节选或截图片段，要明确标记。没有 R05 以外的新游戏模块，没有线上 PR/发布操作。字体不随更新包再分发。没有替换用户任务源、工作项状态、项目资料和素材许可。

## 继续时
先读取本目录规范与安装回执，再看 R05 工程交接。由用户实机验收真实字体、原资产、路由转场、英文和窄屏。通过后记录实际提交号；优先跑通第一期完整候选构建及一次小 PR，而不是马上继续换主题。新项目走 `journal:add`；新组件用通用结构，不为项目 ID 复制整套后台。
