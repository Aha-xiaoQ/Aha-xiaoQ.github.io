# R06 实际验证记录

日期：2026-09-15。状态：本地增量交付，**未推送、未部署**。适用源码：已交付的 R04 多项目框架，可叠加在 R05 工程更新之后。不是完整网站或游戏的端到端验收。

## 实际执行

| 检查 | 结果 | 覆盖边界 |
|---|---|---|
| R06 组件/结构 Node 测试 | 13 通过，0 失败 | 导航名称、模板、通用项目卡、转义、信息预算、作用域、移动布局约束、原转场调用与交接文件 |
| R04 journal 回归 | 48 通过，0 失败 | 原数据契约、任务分页、第三项目创建/归档/撤回、生成一致性等；只更改一项旧名称断言 |
| R05 游戏工程回归 | 34 通过，0 失败 | 原输入/构建/服务/门槛测试，不是完整游戏通关；工程源码保持字节 |
| R05 静态工程检查 | 通过 | 实际运行 game:check，不是已跑 game:audit/build 的完整原游戏 |
| R06 安装器 | 17 通过，0 失败 | 四种历史状态布局的干运行、应用、重复应用、保护和按字节回退；核心冲突、生成页编辑、并发状态变化、后续编辑、符号链接和包校验 |
| 开发栏目离线浏览器组件 | 30 项断言通过 | 搜索、类型/任务筛选、分页、键盘展开、通用资料与 JSON 导出、320/390/768/1440px；内存 fetch/location/history，不是实际地址导航 |
| 共享作品卡离线浏览器组件 | 22 项断言通过 | 两项已公开项目文案、合成游戏/工具条目的布局、入口、旧筛选、单一框架、无嵌套链接、四档宽度 |

以上三组 Node 测试分别运行；不把不同布局重复运行累加为更多功能测试。根 npm test 的旧 R01 脚本并非全部包含在安装夹具中，因此不声称在完整原站跑通了根命令。用户完整仓库中仍应执行根 npm run check 和 npm test。

## 环境与限制

Linux / Node 22.16.0 / 系统 Chromium / Python Playwright。连接器重新确认公开 main 为 ed76f0b668a9e7c1a8d8f71bed2489cb2b063c9e；容器尝试 raw.githubusercontent.com 返回 DNS 失败，未下载完整网站。

浏览器实际尝试 `http://127.0.0.1:4177/notes/` 返回 `ERR_BLOCKED_BY_ADMINISTRATOR`。未修改或绕过策略。随后仅使用 set_content 的离线组件测试，替换地址/网络/历史并将 ESM 作离线作用域适配。未验证浏览器 HTTP 模块加载、原站转场实际呈现、CSP、真实存储刷新或资源请求闭环。

## 预览如何制作

预览聚焦本轮改变的**内容区**，不是完整主站/线上截图。采用已核对的主站主题与内容样式节选、新组件源码、用户先前提供的霞鹜文楷作为私有渲染字体。项目页猫形象只用用户截图的单帧裁片辅助检查位置；安装后的页面仍加载仓库中原有 spritesheet。私有字体及裁片均不放入安装包。游戏/工具用合成文本条目做断言，不生成对外可玩的虚假作品。

## 已发现并修正

旧 CSS 断言把新类名 j-card-body 误判为全局 body 规则，已将新类命名为 j-card-copy，不关闭原有检查。项目卡装饰层从较宽的右侧色块收敛成切角与细错位阴影，留出底部空间，避免抢正文。

## 发布者仍需补验

首页与页头真实资产、原始 Q咪动画、游戏与工具封面；完整网站中英文切换与长正文、前进/后退、刷新与减少动态效果；320px/桌面/200% 缩放；旧草稿与原生下载；Windows CMD、本地权限与 GitHub Pages 发布。真实游戏通关、手柄和音频不在本轮验证范围。

## 复跑

仓库内：`npm run test:r06`、`npm run test:journal`、`npm run game:test`（装有 R05 时）。更新包内：`node --test tests/update.test.mjs`。可选浏览器脚本使用包内 tests/README.md，明确为离线组件检查。

最终 ZIP 已重新解压并核对 CRC、清单、字体排除和安装/回退测试；对应日志保存在包内 validation/。SHA256 是完整性检查，不是数字签名或安全审计。
