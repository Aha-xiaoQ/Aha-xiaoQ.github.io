# R10 / Terraria M02 · 交付验证记录

日期：2026-09-16。状态：本地候选、未推送、未部署。原始底稿仍是用户上传的第三期；从已交付R09/M01继续。原网页稳定游戏、前两期、城堡关、主站CSS/路由不在更新范围。

## 实际执行

|项目|结果|范围|
|---|---|---|
|源码Node完整验证|**85项通过、0失败、0跳过**|原36＋设置/输入32＋架构12＋发布记录5；不是85个新模块|
|新增实际模块|7个，总数15|绑定模型、坐骑键分配、键盘路由、按键捕获、生命周期、设置会话和表格视图，均接入候选|
|原始底稿重建|SHA完全一致|e134fdee21a6dce43585cda93c582ca2c0bf45f325f14cde81e33b80b758f19c|
|完整游戏脚本回放|13阶段无状态差异、无未捕获脚本错误|离线完整原版与候选；部分条件由原测试接口控制，不是自然通关|
|设置窗口DOM交互|**25项通过**|真实点击/键盘事件；控制器样本和延迟File为夹具；原网络请求阻止|
|默认发布门槛|拒绝（预期）|release:check精确候选SHA未审批，未自动将测试证据当发布批准|
|源码ZIP真实解压|check/test/baseline/build复跑通过|从交付ZIP解压运行，不是只跑工作目录|
|源码ZIP重打包|字节相同|377项，8,098,028字节，含校验清单，无字体文件|
|R10更新器测试|**24项通过、0失败、0跳过**|四种旧站点输入，预检查无写入、安装、重复安装、按字节回退、CRLF、冲突、目录新增、悬空链接、源码快照匹配|
|站点集成|terra:verify / terra:arch:check / terra:tasks / terra:pack / journal:build --check通过|用真实M01/M02游戏源码＋明确的历史站点文本夹具|
|安装后源码重打包|与公开M02下载ZIP字节相同|同时核对旧M01下载保持，避免两个源码来源|
|资料页组件检查|**19项通过**|源码与协作两页，单一页头/主内容/页脚、六项导航、下载/路由目标、1440/768/390/320宽度；静态夹具|

四类站点布局是兼容重复，不把相同用例乘算成更多测试。13阶段和25项DOM覆盖是不同内容，不合并成某种“全面通关率”。首次测试时外层清单生成误排除了嵌套manifest.json，现已改为仅排除根清单并复跑24项通过；没有跳过失败测试。

## 身份与交付一致性

源码ZIP SHA-256：`e74ad5a9e2fca5d4001369a3f1930580f9190a35d587519c46b1094ad70e9a26`。

候选单HTML SHA-256：`8765508699756e0c8b3b802b7fe364332f6adb1c9597f604929c1faf2957f3db`。

原M01源码ZIP用于更新前基线，测试重建SHA为 `a9b30f361a8cae1249e43d0ffa3961039b49eac2e2cf7d906e7c492e9af4734d`。原195个内嵌媒体和46个音频条目保留，未引入城堡资源。

源码下载快照固定为交付的换行格式；Windows本地文本CRLF兼容检查只允许换行差异，非文本资产仍比较原始字节。备份与回退保持用户原字节。SHA用于损坏和版本匹配，不是数字签名或安全审计。

## 测试环境

Linux / Node.js22.16.0 / Python Playwright / 系统Chromium。实际HTTP浏览器探测返回ERR_BLOCKED_BY_ADMINISTRATOR，没有更改或绕过策略。游戏通过set_content运行完整候选、保留原CSP与内嵌媒体，并禁用外部请求；不等于HTTP/ESM已完成验收。

站点四种输入由真正的历史R09安装器应用到R08文本夹具生成。其媒体/游戏展示文件存在明确哨兵，不是完整下载的线上站点；M01/M02源码与下载ZIP则是真实文件。站点样式、路由、游戏发布文件和旧任务状态的保护在夹具中按字节比较。

页面预览只用于确认文档结构，缺少品牌字体、Logo文件和页头背景。设置截图来自完整候选离线运行的实际DOM和内嵌游戏素材，没有进行游戏画面重绘。

## 仍须人工/远端完成

Windows原生Node、终端与junction；真实HTTP/ESM、浏览器缓存与下载；设置localStorage刷新、实体手柄/触屏/热插拔；音频听感、自然关卡与隐藏关；第三方素材权利审核；GitHub CI首次运行、分支规则、实际外部PR和Pages发布。本包不创建这些远端对象，也不把配置当运行结果。

默认pad6的上下文共享导入问题已作为有意行为修复记录。33个compat片段仍有旧共享状态；15个独立模块不代表整个游戏模块化完成。未发布的审批和任务状态保持原意。

## 最终ZIP复核

外层ZIP将重新解压后核对CRC与每个SHA256SUMS，再运行24项更新器测试，另建新R09夹具完成安装→terra:verify→journal检查→重复安装→回退。最终精确ZIP哈希与复核结果写在旁置FinalVerification.json，避免ZIP自身哈希循环。validation记录为本次执行日志，不把工作目录路径当用户路径。
