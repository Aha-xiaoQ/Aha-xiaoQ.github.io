# R18 变更

新增站内搜索和按需读取的静态索引；公开目录与当前资料可检索，排除草稿、归档资料和管理记录。搜索与开发筛选保持 URL 状态。长资料增加目录和章节焦点。

主站导航保存滚动及内容焦点，快速连续操作采用最后请求优先。项目加载失败保留静态正文，支持页内重试与正常页面打开。去掉内容页对首页大图的主动预热。

修正构建接线对第二个 nav 的误匹配：搜索辅助入口只在 header 范围处理。修正搜索输入占满宽度导致按钮中文换行的问题。修正筛选改变查询字符串后，旧路径校验阻止滚动记录的问题。

旧三个缓存版本断言随脚本版本改为 r18，图标、主题与其他回归断言保留。未改变游戏版本、玩法或发布状态。
