# R25 实际测试报告

日期：2026-09-19。交付为本地增量源码包，未推送、部署或签发上线批准。

## 测试输入

R24 工作副本由已交付的真实 R19–R24 更新器和数据重建，附带真正 M07、W02 与相应下载。外层网站仍是明确的历史 native-b 文本夹具，不含完整品牌资源。额外的关于页测试段落用于验证迁移保留，不作为页面内容交付。

## 已执行范围

网站 Node 回归、新组件与发布检查的最终执行数量见本报告末尾及包旁 FinalVerification.json。36项新增组件覆盖动作/URL、关于页数据、冲突保护、源/产物一致、静态/动态和公开构建共用。29项更新器检查覆盖全量预检、修改冲突、链接/大小写、迟到写入、备份、回退和幂等。

浏览器使用实际生成视图与新源码：48项布局与交互断言通过；360/390/768/1120/1440五档、两列操作、焦点、真实链接属性、大文字及强制色/减少动态效果。无未捕获脚本异常。没有使用自动上传或真实留言写入。

## 发现并修正

首次回归发现公开构建文件的新增 import 插在 shebang 前面，修复模块语法并复跑发布检查。窄卡片曾可能形成三加一行，增加按容器宽度的两列规则并重新截图检查。最后改动后重新执行全部网站测试，不将首轮失败隐藏为全部一次通过。

## 真实边界

Chromium本地HTTP探测实际返回ERR_BLOCKED_BY_ADMINISTRATOR，没有修改或绕过策略。成功的浏览器组是set_content组件测试，不能作为真实HTTP/ESM/CSP、原生下载权限、SPA/BFCache、完整双语或留言服务可用性的证据。原站字体缺失时预览使用已安装楷体；头像和海报像素来自本次用户截图，仅用于组件预览，不替换站点素材且不入payload。

本轮不修改游戏/地图，采用源文件/下载/主题/路由的前后字节保护，不把旧游戏测试计成本轮重跑。R24上线规划对不完整输入仍应阻断；不能用假媒体或取消检查换取通过。

最终ZIP另解压，检查CRC、清单、禁止字体文件、重新安装、生成一致性、幂等和逐字节回退。Windows、完整资源、真实浏览器往返/下载/留言、全双语和自然游玩仍待实际验收。校验不是数字签名。

## 归档前完整复核结果

|检查|实际结果|
|---|---|
|全部网站 Node 测试|446项通过，0失败/跳过；410项既有网站与发布检查 + 36项新增组件检查|
|R25更新器|29项通过，覆盖冲突、迁移、受保护回退及幂等|
|离线浏览器|48项断言通过，5档宽度，无未捕获异常|
|新R24副本安装|61个文件发生变化；1,647个原有未触及文件字节不变|
|重建与再次应用|site:build/showcase:check等检查成功；再次安装零写入|
|回退|原文件逐字节恢复；再次回退零写入|

最后一次全回归曾有1项新增检查误将类名 media-row__body 当成全局 body 选择器，导致445/446通过。现限定到真实选择器边界，没有取消全局样式约束；新副本复跑446项全部通过。

对不完整外围副本执行R24 release:plan仍返回非零，不会批准发布或补造缺失文件。.local产生的测试审计报告单独保留，回退比较范围为安装前实际文件；没有删除用户文件。

最终ZIP的解压复测、大小和SHA-256见包旁 PixelWorkshop_R25_FinalVerification.json。validation含此次真实日志，不随payload发布到网站。
