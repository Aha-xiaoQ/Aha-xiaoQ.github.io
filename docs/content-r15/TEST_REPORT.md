# R15 · 公开内容与版本一致性验证记录

记录日期：2026-09-17。游戏仍为 **0.4.1 / M05，待人工验收**；R15 为网站及源码文档修订，不是新关卡或游戏玩法版本。未推送、未部署。

## 输入和保留范围

输入为实际 R14 更新包、M05 源码 ZIP、公开内容审阅记录及原有网站脚本。网站外围沿用四种历史文本夹具（native-a、native-b、clarity-a、clarity-b），不是当前线上整站。游戏源码、内嵌素材和新版下载 ZIP 则是真实文件。

构建出的单 HTML 候选 SHA-256：`153942d52d0222f08b2f11a98c62aab075a5506c80cd46deafe20aaeb2ca9a50`，与先前 M05 候选相同。游戏 src、资源、内容与底稿不改；网站 CSS、SVG、小图标、site-router.js 和 site-shell.js 源码保持原字节。原来的第一、二、三期稳定试玩路径及旧下载保留。仅外层页面的必要脚本查询号、当前资料和生成清单随内容更新。

## 已执行

| 检查 | 结果 | 范围 |
|---|---|---|
| 新增内容/版本/任务检查 | 31 项通过，0 失败/跳过 | 同日排序、显式修订号、确定性旧记录顺序、当前版本下载、任务投影、历史源保留、错误提示、教程与手改保护 |
| 原网站组件和工具回归 | 162 项通过 | R06/R07/R08/R12/R13、路径安全与 journal；不是新增 162 项功能 |
| 游戏工程回归 | 153 项通过，0 失败/跳过 | 沿用 M05 的模块、构建、输入、界面等 Node 检查；不等于本轮重新自然通关 |
| 原底稿重建 | SHA 与上传底稿一致 | `e134fdee21a6dce43585cda93c582ca2c0bf45f325f14cde81e33b80b758f19c` |
| 新版源码构建 | HTML 与 M05 逐字节一致 | 文档/任务/打包修订没有改变游戏运行行为 |
| 更新器 | 27 项通过，0 失败/跳过 | 四种输入、干运行、冲突、目录新增、CRLF、悬空链接、并发、额外源码、备份、重复应用、逐字节回退 |
| 浏览器离线内容检查 | 33 项通过，无未捕获脚本错误 | 实际 model/render/runtime、DOM 点击/键盘、任务搜索/分页/筛选/重置、历史折叠、错误重试、四档宽度 |
| 生成一致性 | journal 26 页、集合 3 页检查通过 | 安装后运行真实生成器检查；静态页面和动态组件来自同一份数据 |
| 源码与下载一致 | 安装后重打包与公开源码 ZIP 字节相同 | 新源码 ZIP 内 472 项、8,239,766 字节；源码名保留 M05，新增 R15 文档修订后缀 |
| 发布审批 | 仍未批准 | RELEASE_REVIEW 候选校验值为空，所有人工检查 pending；未用自动测试代替批准 |

四种网站布局是兼容测试，不把相同检查乘算为更多功能。资料范围检查覆盖当前项目页面和 12 篇直接供贡献者使用的源码指南；不是所有历史资料/第三方链接/线上全站的文本认证。

## 具体内容结论

当前版本、主要下载和最新更新由 currentRelease 明确关联；同日更新按 revision 排序，不依赖数组插入顺序。公开任务从工程 TASKS.json 生成，共 19 项；原来 18 个任务 ID 与状态/认领记录保留，补充 M05-UI 本地验证项。local-verified 映射为“待验收”，不显示为已完成。旧 R01 的原始任务与草稿留在管理页历史区域，不自动合并到当前工程状态。

素材许可、AI 辅助贡献责任、能力限制和未验证项保留。当前指南已删除或改写交接口吻；历史溯源、原测试报告和 AGENTS 的维护用途独立保留。

## 环境和限制

Linux / Node.js 22.16.0 / Python Playwright / 系统 Chromium。实际访问 `http://127.0.0.1:4215/notes/mario-mix/docs/terra-source/` 返回 `ERR_BLOCKED_BY_ADMINISTRATOR`；未更改或绕过浏览器策略。

浏览器检查改用 set_content，加载真实生成 HTML、原有 CSS 文本和本次实际模块。模块 import 仅在测试中作离线适配；网络数据、地址、历史和刷新为明确测试替身。截图使用系统字体，不包含完整原站 Logo、背景及霞鹜文楷资源。不是 HTTP/ESM/CSP 加载、原路由转场、真实存储或 Pages 验收。

完整仓库根命令包含未随网站夹具完整收录的早期工具，不能声称已在维护者全仓库跑完根 npm test / npm run check；本报告列明的具体套件已实际执行。维护者应在自己的完整网站执行根检查。

Windows 原生目录链接、真实页面跳转/缓存、原生下载、浏览器历史草稿持久化、手柄/触屏、音频与自然通关仍须实机验证。游戏产物未改变，本轮未重新执行游戏截图/通关，也不以旧游戏截图冒充新测试。没有操作远端、创建 Issue、合并 PR 或填写批准。

## 测试中修正的情况

历史用例中缓存号与特定 M04 下载名的断言已改为当前数据来源，原转义、安全与保留规则不删除。源码任务计数由18改为19，对应明确新增 M05-UI，不把旧任务伪装完成。文档入口检查最初误要求所有技术文档包含开发试验地址，现限定为实际启动教程；其余文档继续检查公开措辞。最终复核首次把 ESM 的 play.html 与单文件 HTML 比较，已修正为比较同一种单文件产物；ESM 入口与单文件不是同一个文件。

## 最终 ZIP 复核

最终外层 ZIP 已另行解压，CRC、SHA256SUMS 和包清单核对通过；解压目录的27项更新器测试通过。随后在新建的 R14 文本网站副本中安装，内容31项、网站162项、源码153项、底稿重建和产物检查均通过；源码重打包与公开下载一致，生成页检查、重复安装和回退通过。独立源码 ZIP 也单独解压运行 verify、prepublish:check 和 pack，重打包字节相同。最终精确 ZIP 哈希与步骤结果存为包旁 `PixelWorkshop_R15_FinalVerification.json`，避免把文件自身校验值嵌回 ZIP 形成循环。

校验值仅检测损坏/内容差异，不是数字签名或安全认证。
