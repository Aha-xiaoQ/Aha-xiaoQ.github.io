# R13 / Terraria M04 · 交付验证记录

记录日期2026-09-17。候选版本0.4.0，供人工验收与源码协作；未推送、未部署。用户指定的第三期上传包仍为唯一底稿。城堡、前两期和线上第三期稳定文件不覆盖。

## 已实际执行

|检查|实际结果|边界|
|---|---|---|
|源码Node回归|138项通过，0失败/跳过|M03的118项＋20项物品/生命周期/契约测试，不是模块数量|
|独立模块与桥接|24模块、4桥接、33兼容片段|新增2模块真正接入旧第三期；剩余旧共享状态未假报为清空|
|原始底稿重建|SHA完全一致|e134fdee21a6dce43585cda93c582ca2c0bf45f325f14cde81e33b80b758f19c|
|原第三期完整脚本离线回放|13阶段一致，无未捕获错误|部分条件由测试接口设置，非自然通关|
|设置/原型入口/新贡献演练|25项/21项/7项通过|完整候选＋真实DOM；手柄与外部贡献者是测试样本和作者模拟|
|普通玩家入口|6项通过|默认隐藏实验入口；显式测试上下文保留，不是dev=1真实URL验收|
|资料页与旧网站组件回归|162项通过，其中16项新增|R06/R07/R08、安全、journal和R12规则保留；R12小图标与CSS不变|
|更新器|28项通过|四种R12历史输入、源包重建、冲突、CRLF、悬空链接、并发、重复安装及逐字节回退|
|资料页原生DOM|23项通过|3主入口、历史默认折叠/键盘展开、真实zip链接、旧资料直链、1440/768/390/320宽度|
|生成一致性|journal pages26，site pages3，均changed0|实际旧生成器和本轮修改；网站外围仍是文本夹具|

四种网站布局为兼容重复测试，不把相同用例乘算为额外功能。138项中324个武器组合和64个箱子组合只是输入覆盖，不额外相加。

## 已修正而不是掩盖的检查

新增真实模块/桥接/任务后对应计数由22/2/15更新为24/4/18，原权限、依赖与证据检查不删。物品测试跨VM对象比较使用规范序列化避免原型误报。首次源码解压验证暴露18条任务与旧15条断言不符，已修正后重跑；未把失败批次当成通过。

历史R06/R12中只有脚本缓存号断言随本次r13加载链同步；CSS与图标仍r12。保存项目历史时使用目标仓库的实际配置原字节，而非固定某个A/B分支；维护者追加的更新记录与任务源指向保留。

## 最终包流程

最终源包必须解压执行verify、prepublish:check并重新pack，源码ZIP应字节相同。外层ZIP另解压核对CRC、SHA256SUMS和manifest，再执行28项更新器测试；在新R12文本夹具安装后跑源码测试/构建、162项网页组件、journal/site生成检查、源码pack一致性、重复安装和回退。

最终精确源包/候选/外层ZIP哈希与步骤结果放包旁FinalVerification.json，避免将ZIP自身哈希写入ZIP。包内validation是本次实际日志；源目录日志与最终ZIP解压复核分开记录。

## 不可据此宣称

Linux、Node22.16.0、Chromium/Playwright。实际HTTP探测4196/play.html仍被浏览器策略拒绝，未绕过。游戏离线完整脚本保留CSP与内嵌媒体，远程请求禁用；网站页面使用真实生成HTML与共享样式文本，但无完整品牌资产/路由网络。没有Windows原生、物理手柄/触屏、持久存档、音频听感、自然通关、远端CI/Pages、首次外部PR或素材许可核准结果。

原泰拉角色/武器/坐骑尚不能任意套新地图，platform-v1只是关卡接入实验。不称完整1-4或通用引擎。`prepublish:check`只检查自动产物一致性；`release:check`在人工记录缺失时拒绝，不能因准备发布而自动签字。

## 保留范围

主站CSS、R12小手柄与浏览器图标、site-shell.js和site-router.js源码、旧任务文件、R05工程、稳定游戏运行与下载不改。外层HTML只同步必要的脚本缓存号。原M01-M03下载保留，旧文档有历史提示且直链可访问。历史文本和原权利声明随包，不新增第三方许可授权。
