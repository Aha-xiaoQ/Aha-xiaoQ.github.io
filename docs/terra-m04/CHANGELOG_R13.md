# R13 站点接入变化

- 更新第三期工程为M04/0.4.0，增加prepublish:check与test:loot的根命令。
- 当前源码仅一个M04主下载；参与与关卡接口另页，旧文档折叠。完整旧项目配置归档，不撤销任何旧路由/下载。
- 通用文档模型增加可选archived/action字段；旧项目缺省行为不变。
- 动态模块与HTML引用使用r13缓存号，R12图标、所有共享CSS和路由动画源码不变。
- 原R06/R12测试只更新JS缓存号断言，CSS缓存r12与图标约束保留。没有删除历史测试。
