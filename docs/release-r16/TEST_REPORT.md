# R16 / M06 本地测试记录

日期2026-09-17。本地候选，未推送/未部署。游戏输入是真实R15/M05模块源码与原第三期内嵌媒体；网站外围是由原R15更新器生成的四类文本夹具，不是完整线上网站。

## 已执行套件

- 游戏Node171项：153原回归、15引导/生命周期、3过期产物保护，通过。
- 完整原游戏与候选13阶段离线状态一致；部分条件由原诊断接口设置，不是自然通关。
- 新操作速查44项DOM；原显示35项、设置25项、关卡样例21项、普通入口6项回归通过。新旧脚本都保留原CSP，远程请求阻止。
- 网站203项：原193项＋10项主要页面/文档复制/元数据检查通过。
- 网站离线DOM41项：当前版本、源码链接、实际命令复制/拒绝/迟到回调、任务20项搜索分页、历史折叠、错误恢复及四档宽度，通过。
- 更新器27项已通过；四类输入不累加为四倍功能数量。最终解压结果另随validation和包旁FinalVerification记录。

## 修正记录
首次提示条被display:flex覆盖hidden，补显式规则。新浏览器改键用例初次选择了已被坐骑切换占用的V键，改用可用的Y并核对真实绑定，没有绕过冲突规则。旧版本断言改为读取release元数据；源页增加明确“待验收”。R07悬空链接检查继续使用lstat，未删测试。

## 边界

真实浏览器HTTP访问返回ERR_BLOCKED_BY_ADMINISTRATOR，未更改或绕过策略。DOM通过set_content执行完整候选；pagehide.persisted为合成事件，不能当作实际BFCache验证。网站地址/历史/fetch为测试替身，图标真实但完整页头/字体/背景未加载。Node HTTP端口测试不替代浏览器加载链路。

未运行：Windows原生、真实页面转场/缓存、实体手柄/触屏、持久存储刷新、音频听感、自然通关、远端CI/Pages、外部PR、素材授权。preflight检查本地主要页面及产物，不是全站性能/无障碍认证，不报告伪造Lighthouse分数。

## 最终复核

外层ZIP与源码ZIP另行解压，核对CRC/清单、跑安装器与源码verify/prepublish，再安装→网页检查→重打包一致→重复安装→回退。最终精确文件SHA保存在包旁FinalVerification，不写回ZIP避免循环。
