# R12 实际验证记录

日期：2026-09-17。本地交付候选，未推送、未部署。范围：开发目录的游戏／网站小图标，未改变游戏版本。

## 已实际执行

| 项目 | 结果 | 范围 |
|---|---|---|
| R12 新组件与集成测试 | 12 项通过 | 两类小图标、浏览器构图、展示尺寸、转义／筛选保持、缓存链、原路由和交接约束 |
| 已有组件与工具回归 | 134 项通过 | R06 13＋R07 42＋R08 13＋安全 18＋journal 48；不包括新游戏通关 |
| 更新器 | 19 项通过 | 四类 R11 文本输入的检查、应用、重复应用、逐字节回退；CRLF、文件冲突、生成页编辑、悬空链接与写前状态变化 |
| 浏览器组件 | 25 项通过，无未捕获错误 | 使用当前 renderer/runtime 的真实函数及 DOM 事件，离线数据／地址／翻译替身；筛选、搜索、重置、挂载往返、图标实际 SVG、四档宽度及键盘焦点 |
| 静态生成 | journal changed:0 / removed:0 / pages:25；site changed:0 / pages:3 | 真实旧生成器的当前改版，文本测试网站不是完整线上网站 |
| 实际游戏工程保护 | 453 个 packages 文件、7 个下载文件、8 个内容文件在独立验证副本前后 SHA-256 一致 | 来源是已交付 R11 数据与真实 M03 源码；游戏逻辑未修改，因此不冒称本轮重做了全部游戏回放 |

计数中四种兼容输入是重复验证，不乘算成更多独立组件测试。完整原站根命令仍应在维护者副本执行，没有用文字夹具冒称线上验收。

## 修正过的测试
R08 有一项断言硬性要求 PLAY，恰好与用户本次明确要求冲突：改为禁止 PLAY 且要求 compact。R06 缓存断言原来硬性要求 R08 查询号，更新为 R12。R12 初稿的静态检查误把按变量比较项目 ID 当作硬编码，已限定为字面量 ID。没有删除测试或跳过失败用例。

## 可见效果与范围
手柄采用 R06 的简洁轮廓与小尺寸方向，浏览器补齐地址栏、页面分区和指针。不含大字横幅、底部标语或新的动画。标题、简介、状态、链接不变。改动后的媒体识别区更紧凑，不使用宽幅背景板。

主站 CSS、site-router.js 与 site-shell.js 源码、任务数据、下载和 M03 文件未改。为了避免缓存旧 renderer，动态 import 和目录 CSS 升级查询号；其他 HTML 只升级 router 脚本查询号，正文与布局不改。原生成清单同步记录实际字节。

## 浏览器边界
Linux / Node 22.16.0 / 系统 Chromium。真实请求 http://127.0.0.1:4212/notes/ 返回 ERR_BLOCKED_BY_ADMINISTRATOR；没有更改或绕过策略。随后在离线 set_content 中检验真实模块的 DOM 行为。网络、地址、历史及语言对象是测试替身，不能证明真实跨 URL 动画、ESM/CSP 网络、存储或 Pages 已验收。

截图只展示本次修改的内容区，使用原站样式文本节选、用户已有私有霞鹜文楷与这次实际 SVG。字体仅在内存／私有测试目录用于渲染，不进入更新包；没有上传任何字体。未使用假猫图、海报或新游戏资源。

## 最终压缩包
最终 ZIP 将另行解压验证 CRC、SHA256SUMS、payload/tooling 清单，并重跑更新器和一次独立完整安装→146 项组件工具验证→生成检查→重复安装→回退。最后结果以包旁的 FinalVerification.json 为准，避免把 ZIP 自身 SHA 写回 ZIP。

Windows 原生运行、完整主站品牌资源、真实转场、缓存、原页面入口及上线结果仍需维护者验收。无远端操作。完整性清单不是数字签名或安全审计。
