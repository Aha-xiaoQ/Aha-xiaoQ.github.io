# 发布前验收

打开 /notes/：两张卡片均显示克制的小图标。手柄不超过 96 CSS px 展示宽度；网站图标在同一个展示槽中具有浏览器语义。不得出现 PLAY／BUILD 大字或图标下微小标语。

由首页进入、分类切换、搜索和项目详情返回，图标不得恢复旧版。检查 320/390/768/1440 CSS px 与系统减少动态效果：无横向溢出，不新增图标动画，原键盘焦点与入口有效。

核对正文、状态、任务与 M03 资料和源码下载均保留；源下载包与游戏工程不能发生 R12 改动。运行 test:r12、test:r08、test:journal、site:check 和原根命令。Windows 双击、真实主站路由、字体、背景、缓存及 Pages 部署需在维护者环境实际确认；自动测试不替代这些项目。

确认后由维护者手动提交。不要把更新 ZIP、测试站点副本或仓库旁备份发布为网站资源。
