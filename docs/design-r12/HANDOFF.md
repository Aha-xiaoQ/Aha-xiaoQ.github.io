# R12 交接 · 小图标恢复

日期：2026-09-17。状态：本地候选，待维护者发布；不是新的游戏进度。

用户明确认为大手柄＋PLAY、大浏览器＋BUILD 属于风格退化。R06 的小型倾斜手柄才是本次恢复参考；当时需要改进的是右侧抽象矩形的网页辨识度。本包保留简洁手柄轮廓，浏览器图标由地址栏、窗口栏、内容区和指针组成。两者均为 96×72 展示槽，图形自身更小，没有标语。

## 已定边界
- 游戏与网站目录卡片使用 compact 图标，不能为了显眼改回 320×180 大图横幅。
- 图标只是辅助识别；标题、状态、介绍、项目链接仍是主要信息。
- 图标与两张卡片配色保持米白／红／黑。正文与导航保持原样。
- 其他类别原有插画、工作流项目图、游戏列表不修改。
- 游戏工程仍以 Terraria M03 文档为准；这轮不更新模块、关卡、任务状态或源码下载包。
- scripts/lib/safe-path.mjs 不在覆盖清单，工具副本沿用已经修正悬空链接检查的实现。

## 代码定位
assets/journal/render.mjs 的 projectEmblem；assets/journal/journal.css 的 R12 局部规则；assets/journal/icons/controller.svg / web-studio.svg。

## 缓存与生成
runtime 只更新 render import 的查询版本。两个原构建器仅更新相应资源查询号，使重新构建后不倒回旧缓存。site-router.js 源码未改。直接打开与站内筛选都使用同一 renderer。其他 HTML 只更新必要的脚本查询号。

## 下一轮
先核对维护者实际 diff、截图与应用记录，再继续游戏架构任务。不应把视觉补丁标成游戏模块升级，也不应覆盖 M03 未上传改动。读取 TEST_REPORT.md 区分当前实际通过与需要实机验收的项目。
