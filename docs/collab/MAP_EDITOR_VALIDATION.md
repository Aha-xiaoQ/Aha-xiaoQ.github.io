# 地图工坊：实施与验收记录

2026-09-19 · 本地候选，未推送或部署。目标：下一关能够用可视化工具制作地形，不以展示图代替编辑器。

## 已实现

- 空白工程、32 关 / 96 区域底图副本导入；原始参考数据保留。
- 画笔插值、橡皮、矩形、桶填充、吸管、框选、拖动、复制粘贴、删除。
- 地形、装饰、对象三层，当前层锁定；网格、缩放、平移。
- 地图尺寸修改，越界时拒绝而非静默裁剪；坐标属性修改。
- 撤销/重做、本机自动保存、工程 JSON 往返。
- 结构图、示意纹理效果图、隐藏标记展示；奖励及机关记录可查看，关联地块整体移动/删除同步标记。
- 实际复用现有平台驱动进行单区域地形试跑；开发 ZIP 通过现有目录加载器验证。
- 一键启动脚本与中文使用说明。

## 证据

- 新增 10 项测试通过。包含 32 关全部 96 区域往返；原数据不变；地块挖孔、越界画笔、填充边界、撤销、尺寸保护、奖励关联、损坏输入、ZIP CRC。
- UI 事件测试执行真实事件处理器、使用最小 DOM 替身，覆盖笔画取消与自动保存、多指所有权、图层锁定、撤销/重做。它不是实际浏览器验收。
- 真实平台驱动模拟 180 步，新画地面正确承托角色，未损失生命。
- 开发数据复制到临时第三期 content 副本，loadExtensionPack 成功识别新关卡，sealed legacy 校验通过。原源码未修改。
- 复制的运行模块与来源字节相同。没有改变现有游戏运行文件。
- worlds:check 与 worlds 测试通过；全站检查通过。全量 npm test 766 项通过（含新增 10 项）；后续局部校验亦通过。
- HTML 质量检查 0 错误；检查器对外置 CSS/JS 的响应布局、focus 和 canvas 检测产生 3 条提示，相关实现已在独立文件中。
- 本地 4183 编辑器入口 HTTP 200。

## 独立审查与修复

Code Auditor（码鉴），运行 editor_review，只读审查两轮：

1. 修复自动保存写入尚未提交笔画、取消后刷新又出现的问题。
2. 修复第二根手指覆盖首个笔画撤销快照的问题。
3. 修复负坐标笔画误写地图边缘的问题。
4. 关联奖励、机关随整块移动/删除；部分绘制保护关联组件；粘贴运动平台校验范围。
5. 独立键盘事务先取消当前指针笔画，避免交叉撤销。

复审未发现残留 P1 保存/导出阻塞。不是视觉验收结论。

## 未完成和发布边界

- cua 浏览器连接失败。Windows Computer Use 也因无法可靠确定浏览器 URL 而停止，未绕过。无法获得真实桌面/手机截图和实际点击证据，Pagewright / Visual Arbiter / Usability Validator 的渲染验收仍待完成。
- 当前效果图是示意材质，不是最终美术或原版截图；隐藏内容仅按数据标记，不是完整路线连接图。
- 未开放完整机关属性编辑、跨房间游戏流程及 Tiled 回写。水下和跨房间管道不能在单区域平台试跑中冒充可用。
- 金色块、砖纹块是外观，不代表奖励与破坏行为。基础运行时不执行参考标记或装饰。
- 工程自动保存只有一份本机槽位，跨设备需要导出文件；并非实时协作。
- 未替换已有下载 Starter，也未推送线上。新功能只在本地预览与源码候选中。

## 下一次验收

打开 /packages/mario-mix-worlds/atlas/editor.html。画出地面、缺口、单向平台，放出生点和出口；验证试跑、撤销、保存刷新、导出重开、开发包接入。桌面和窄屏各检查一次。实际操作通过后，才将该工具用于下一关正式制作并发布更新的开发包。

## 2026-09-19 地图默认区域修正

图集与编辑器的 1-2、2-2、4-2、7-2 默认显示 area-1 主地图；area-0 入口转场保留为可选区域。显式区域链接及保存的选择优先，游戏入口与原始地图数据不变。相关编辑器和区域选择测试 12 项通过。本地预览同步，未推送；真实浏览器验收仍待完成。

