# 网站关键路径与全面检查 · 2026-09-19

检查基线：`ce0326fde926baae513b76e547962ff40a9afe52`。用户要求检查现有网页问题，延续已授权的修复与推送；不调整游戏玩法、主题或历史页面内容。

## 已修复

README 中合法的仓库目录链接被协作检查误当成网页目录，错误要求存在 `index.html`，导致 GitHub Collaboration checks 失败。现在 Markdown 可以链接到真实存在的目录，HTML 仍要求入口页；不存在的目录与越界路径仍报错。英文 README 加入检查清单。新增回归测试覆盖这些差异。

## 已验证

- `npm run check`、`npm test` 通过；测试输出合计 756 项，无失败。
- `npm run collab:build`、`npm run doctor` 通过。
- 全站静态入口检查：39 页、268 个引用文件，无错误。大图体积警告保留，不为消除警告擅自修改素材。
- 线上首页、主要栏目、三个游戏介绍页、Q 咪页、开发资料页和中英文 README 返回 200。
- 48 个实际下载链接的线上 HEAD 请求均返回 200；抽查 Map_1-3_W02 在线完整下载 15,125 字节，与本地 SHA-256 一致。对应本地 ZIP 全部通过目录、大小与 CRC 校验，合计 2,973 个文件。此结果不等同于逐个完整下载线上 ZIP。
- 第二、三期试玩 HEAD 返回 200；20 秒及 55 秒两次完整读取均超时，不能仅凭 HEAD 认定加载性能正常。

## 尚未通过与限制

- 浏览器工具连接失败，无法取得浏览器会话。本轮未完成实际点击、前进后退、语言切换、手机布局、动画与实际字体渲染的复查。自动测试不能替代这些交互验收。
- 第一期开发候选 `npm run game:build` 被 HTML 基线校验拦截；运行时代码基线已通过此前一步校验。未放宽锁定哈希、未覆盖线上游戏。需要单独核对入口包装差异，再决定适配方案。
- `npm run release:check` 未通过，共 14 条：5 个 `packages/mario-mix-levels` 资料资源各报内部引用及缺失；首页报 main/h1 非唯一；两张 Quina 工具页面报 main 非唯一。它检查独立发布投影，不代表这些资源在当前整仓 GitHub Pages 部署中返回 404。需修正发布资源映射并逐页区分 noscript 后备内容与真实重复结构。
- 未进行游戏通关、实体手柄、音频及素材许可全面验收。

## 后续重点

恢复浏览器后补做实际交互；单独修复独立发布包阻塞项，并保持候选游戏的基线保护。网页外观和现有游戏本体本轮未改动。
