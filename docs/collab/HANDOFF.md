# 交接记录 · collab-r01

记录日期：2026-09-14。状态源：`collab/project.json`。本轮基线：`ed76f0b668a9e7c1a8d8f71bed2489cb2b063c9e`。

## 用户确认的工作方式

在本地副本/读取到的文件上改造，提供可下载包，由用户检查后手动推送。后续每一轮继续采用相同流程，不直接操作远端写入。目标是新对话或中断后依靠仓库记录继续，不依赖聊天历史。

## 这轮已交付

独立开发中心、保留原笔记的导航接入、三期入口说明、可导出的本地任务草稿、状态数据模型、启动与校验命令、贡献/安全说明、Issue/PR 模板、限定范围的新协作工具许可、低权限 CI 文件、安全增量交付工具和续接文档。

整包 clone 与 archive 下载在制作环境失败。通过 GitHub 只读连接读取了源文件与树元数据；两个实际修改的原文件（README 与 site-shell）已在本地重建，并且 Git blob SHA 与远端完全相同。本轮交付增量包，不伪称完整仓库副本。原游戏本体未修改、未重新打包，也未在本轮完整运行。

## 没有做的事情

没有推送、提交远端 PR、创建 Issue、更改仓库权限或部署网站。没有统一游戏引擎、拆分第三期巨大 HTML、修复游戏玩法、迁移存档或实现全量游戏构建。没有完成素材许可审计。CI 文件在 GitHub 上尚未首次运行。

## 下一步顺序

1. `OPS-001`：用户使用包内检查/应用工具，校验本地文件后推送；验证 `/dev/` 与原站入口。
2. `COL-004`：核对远端检查运行、Issue 表单和 CODEOWNERS；再按维护者清单设置保护规则。不要直接宣布保护已生效。
3. `SRC-001`：读取新的 main 和用户最新离线版，明确第三期开发基线。仓库已包含 mario-mix-2 和 mario-mix-3，不得退回“只有第一期”的判断。
4. `TEST-001`：先录制操作与通关回归，再做 `ARCH-001` 输入模型抽离试点。

每项完成后更新 `project.json.nextTask`、`updatedAt`、`deliveryStatus`、任务状态和验收依据，并重新生成。不要永远保留本页的旧“下一步”。

## 新对话的恢复方式

运行 `npm run handoff` 或点击开发中心“复制新对话续接说明”。助手先读取本文件、AGENTS、当前任务、验证报告和远端 main，然后报告差异再继续修改。若 main 上还没有协作文件，说明上一个包尚未推送或推送到其他分支；核对用户给出的包/分支，不另造一个“最新版”。

## 当前必须避免的误区

开发中心是静态快照，不是 GitHub 项目 API 的实时镜像。网页保存是本地草稿，不是提交。工具测试通过不是游戏手感验收。元数据确认不是完整源码审计。新增协作工具的 MIT 不覆盖整个游戏。


## 2026-09-19 网站链接修复

用户授权检查现有网页并修复推送。以 337af95 为基线，修正 terra-chapters 的四个旧 SDK 文档链接，改为现行 stages 指南、契约与验收清单。修正 launch 审计将 noscript 备用页面误算为重复主内容的问题；真实重复仍拦截。39 个页面、268 个引用文件审计无错误。未改变公开文字、字体或游戏玩法；当前工作状态文案保留。

## 2026-09-19 全面检查补充

修复 README 目录链接被误判为网页入口的问题，英文 README 纳入检查；全量检查与 756 项自动测试通过。完整结果与未通过项见 [全面检查记录](WEBSITE_AUDIT_2026-09-19.md)。浏览器连接失败，实际交互未复验；独立发布包检查和第一期候选构建仍有阻塞，未放宽校验。用户本轮延续授权修复推送。

## 2026-09-19 地图工坊本地候选

最新任务是先规划、参考成熟编辑器，再完成下一关可用的可视化地图编辑。已实现基础编辑、保存、三视图、现有驱动试跑和开发包导出；原游戏与下载包未覆盖。详见 [实施与验收](MAP_EDITOR_VALIDATION.md) 和 [方案](MAP_EDITOR_PLAN.md)。浏览器连接与 Windows URL 校验阻断了真实界面验收；待补操作与截图后再发布。预览源在 D:/Code/xiaoq-homepage-push-r25，运行镜像 D:/Code/xiaoq-homepage-github-main-r08-review，端口 4183。

## 2026-09-19 地图默认区域修正

图集与编辑器的 1-2、2-2、4-2、7-2 默认显示 area-1 主地图；area-0 入口转场保留为可选区域。显式区域链接及保存的选择优先，游戏入口与原始地图数据不变。相关编辑器和区域选择测试 12 项通过。本地预览同步，未推送；真实浏览器验收仍待完成。

