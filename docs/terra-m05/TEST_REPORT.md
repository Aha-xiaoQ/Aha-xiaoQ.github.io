# M05 / 0.4.1 UI 精修 · 实际验证记录

日期2026-09-17。Linux / Node22.16.0 / Python Playwright / 系统Chromium。本地候选，未推送/部署。输入是真实M04工程和上传底稿，网站集成使用真实R13生成器及明确文本夹具，不是完整线上副本。

## 源码与游戏
- **153个Node测试通过，0失败/跳过**：原138回归+15个UI模型、绘制、输入边界与样式检查。27模块/6桥接/33兼容片段；wrapper赋值计数41不增加。
- 原版重建SHA与上传HTML一致；候选构建支持单HTML与模块网页，不把两个形式都说成浏览器HTTP已验证。
- 完整原版/候选脚本离线对照13阶段，无状态差异；随机/时间/进入隐藏场景等条件由测试钩子控制，不是自然通关。
- 新UI离线DOM **35项检查**：实际开始/专注/改键/显示偏好，键盘Space/Escape、快捷提示随绑定更新、原工具点击、五档视口及大号对话框；没有未捕获脚本错误。
- 既有设置窗口25项、关卡原型21项、普通玩家入口6项回归通过。开发实验仍需显式入口。

## 界面测试怎样运行
file或本地HTTP导航受环境策略ERR_BLOCKED_BY_ADMINISTRATOR限制，未更改/绕过。使用set_content执行完整候选HTML与原内嵌素材，保留CSP，阻止远程请求。默认菜单截图不用测试标记；诊断对照才启用测试接口。资源状态诚实显示未加载远程素材。

大号偏好对CSS菜单正文测试20px；不声称像素HUD放大。视口320/390/768/1440以及844×390未横向溢出，仍需真机可读性检查。偏好纯模型测了独立key与存储失败，浏览器刷新/权限仍待实机。

## 修复过的制作问题
最初新挂载放在第三期内层闭包之外，出现t21KeyLabel未定义，已移到48片段内部后复跑。原测试等待字符串触发CSP限制，改用函数形式，不放宽CSP。侧栏将intro搬入容器会破坏原t20选择器，现仅包裹标题/kicker，intro仍为直接子节点。

快捷提示刷新由原生dialog close事件驱动；复跑曾因立即读取DOM产生时序失败，用等待可观察到的U键提示替代即时断言，没有改动CSP或关闭用例。

## 最终文件复核
源码ZIP须另解压跑verify与prepublish:check，再pack逐字节一致。更新ZIP须另解压核对CRC、清单、执行安装保护测试，安装到R13文本夹具后重新生成网页并验证当前源码下载匹配，重复安装和回退。精确最终结果与SHA放交付包旁FinalVerification.json，包内validation保存打包前真实结果；不把制作目录通过当解压通过。

## 尚未验证/不包含
Windows原生CLI/junction、真正HTTP/ESM加载/CSP网络、实体手柄/触屏、刷新持久设置、音频听感、自然通关、主站品牌资源和Pages部署、外部PR、第三方许可审批均未验收。新增减少动效不关闭游戏震动/世界闪光。未改线上游戏、城堡、网站主题、R12图标/路由。原媒体195项/音频46条保留，没有新字体或外部美术。
